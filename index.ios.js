/**
 * Weather Application
 */
import React, {Component} from 'react';
import {AppRegistry, StyleSheet, Text, View} from 'react-native';

import Application from './weather/Application';

AppRegistry.registerComponent('weather', () => Application);
